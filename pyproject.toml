[project]
name = "yande-re-spider"
version = "2.0.0"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.4.2",
    "loguru",
    "requests[socks]",
    "rich",
    "pathvalidate",
    "sqlalchemy>=2.0",
    "sqlmodel>=0.0.14",
    "mariadb",
    "fastapi>=0.104.0",
    "uvicorn[standard]>=0.24.0",
    "python-multipart>=0.0.6",
    "websockets>=12.0",
    "pyyaml>=6.0",
]

[tool.uv]
dev-dependencies = []
