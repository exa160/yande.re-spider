services:
  yande-spider:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./config/config.yaml:/app/config/config.yaml
      - ./data/yande_data.db:/app/data/yande_data.db
      - ./downloads:/app/downloads
    restart: unless-stopped
