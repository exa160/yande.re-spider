name: Docker Image CI

on:
  pull_request:
    branches: [ "next" ]
  push:
    tags: [ 'v*.*.*' ]
jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      # - name: Set up QEMU (for multi-platform builds)
        # uses: docker/setup-qemu-action@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Extract Docker metadata
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: |
            ${{ secrets.DOCKERHUB_USERNAME }}/picture-manager
          tags: |
            # Generate commit SHA tag (format: git-<short SHA>) only on PR events
            type=sha,prefix=git-,enable=${{ github.event_name == 'pull_request' }}
            # Generate semantic version tags only on tag push
            type=semver,pattern={{version}},enable=${{ github.event_name == 'push' && github.ref_type == 'tag' }}
            # Add 'latest' tag only on tag push
            type=raw,value=latest,enable=${{ github.ref_type == 'tag' }}

      - name: Build and push Docker images
        uses: docker/build-push-action@v7
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
